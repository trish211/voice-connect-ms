//The Service model is a Java Enum which defines the collection of constants
package model;

public enum Service {
    Fixed,
    HomeDevices,
    Internet,
    Mobile,
    Sensis,
    WhitePages
}
